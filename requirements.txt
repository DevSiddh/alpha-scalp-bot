# Alpha-Scalp Bot — Python 3.11 dependencies

# Core runtime
ccxt>=4.2.0
pandas>=2.1.0
numpy>=1.26.0
loguru>=0.7.2
python-dotenv>=1.0.0
httpx>=0.27.0
websockets>=12.0
aiohttp>=3.9.0
aiohttp-retry>=2.8.3

# pandas-ta is no longer on PyPI for Python 3.11.
# A compatibility shim (pandas_ta.py) wraps the `ta` library instead.
ta>=0.10.0

# Tests
pytest>=7.4.0
pytest-asyncio>=0.23.0
anyio>=4.0.0
