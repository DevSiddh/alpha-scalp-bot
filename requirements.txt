# Alpha-Scalp Bot — CI + test dependencies (Python 3.11)
#
# pandas-ta is NOT listed here because:
#   - PyPI only has 0.4.x+ which requires Python >=3.12
#   - conftest.py injects a no-op stub so tests run without it
#   - Production VPS: pip install pandas-ta==0.3.14b0 --index-url ... manually
#
ccxt>=4.2.0
pandas>=2.1.0
numpy>=1.26.0
loguru>=0.7.2
python-dotenv>=1.0.0
httpx>=0.27.0
websockets>=12.0
aiohttp>=3.9.0
aiohttp-retry>=2.8.3
pytest>=7.4.0
pytest-asyncio>=0.23.0
